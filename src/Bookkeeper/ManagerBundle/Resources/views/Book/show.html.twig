{% extends '::base.html.twig' %}

{% block body -%}
	<h1>Book #{{ book.id }} - {{ book.title }}</h1>
	<h3>No. of pages: {{ book.pages }}</h3>
	<p>{{ book.description }}</p>

	<ul>
	    <li><a href="{{ path('book') }}" title="">Book Manager</a></li>
	    <li><a href="{{ path('book_edit', {'id': book.id}) }}" title="">Edit Book</a></li>
	</ul>
{% endblock %}
